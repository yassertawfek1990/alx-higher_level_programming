#!/usr/bin/python3
"""Defing NumPy."""
import numpy as np


def lazy_matrix_mul(m_a, m_b):
    """Retuices.

    Args:
        m_a (lints/floats): Tst matrix.
        m_b (lints/floats): Thatrix.
    """

    return (np.matmul(m_a, m_b))
